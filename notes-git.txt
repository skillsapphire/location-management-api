1. git clone  https://github.com/skillsapphire/location-management-api.git

2. Default branch will be master
Create a new local branch:
git checkout -b <branch-name> # Create a new branch and check it out
git checkout -b 1_feature_setup
git add .
git commit -m "new branch created"
git push --set-upstream origin 1_feature_setup

=====================================================

create a new repository on the command line
echo "# todo-complete-spring-boot" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/skillsapphire/todo-complete-spring-boot.git
git push -u origin main

…or push an existing repository from the command line
git remote add origin https://github.com/skillsapphire/todo-complete-spring-boot.git
git branch -M main
git push -u origin main

